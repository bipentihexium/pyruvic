#ifndef __PROJECT_UTILS_HPP
#define __PROJECT_UTILS_HPP

#include <string>

void new_project(const std::string &);
void load_project();
void clean_build_files();
void build_project();
void run_project();

#endif
